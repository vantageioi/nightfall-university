# Journey Release QA Notes — 2026-08-27

The production deployment `dpl_4hp4doSYVh42A6kVGAWRzX8cRFYw` reached `READY` and owns `nightfall-university.vercel.app`. Its canonical `/api/health` endpoint responded successfully with the database connected.

A non-mutating canonical browser check of `/dashboard?release=journey-notebook-20260827` rendered the preceding Journey Home client shell after a hard refresh. The newly generated Vercel deployment manifest contains the new `You have a direction.` Journey source, so this is recorded as a **canonical-origin PWA service-worker cache observation**, not as confirmation that the server deployment omitted the implementation. The deployment-specific URL is protected by Vercel Authentication and could not be used to visually inspect the bundle without a Vercel authenticated session.

No student record, programme state, document, message, deadline, or preparation transition was changed during visual QA.

After deployment `dpl_8s77paNTRPN64LwiY7DiToTDeg58` added `skipWaiting`, `clientsClaim`, and obsolete-precache cleanup to the PWA policy, a second non-mutating canonical check at `/dashboard?release=journey-notebook-final-20260827` rendered the new Journey home. It displayed the `NIGHTFALL / YOUR JOURNEY` heading, `You have a direction.` introduction, real zero-state counts for shortlisted, active preparation, and upcoming dates, and the explicit quiet-state message. This confirms the earlier visual mismatch was resolved without changing student data.

For the Application OS release, the canonical `/dashboard?tab=calendar&release=application-os-20260827` route was opened as the existing signed-in student without changing a record. The deterministic Journey guard correctly redirected this profile to its source-first discovery fallback because it has no saved programme, active preparation path, or student-reviewed date. The visible Journey home correctly showed the real zero state and its `Review my first options` next action. Timeline, Application Detail, Watch, and Communications are therefore covered by type checks and focused component tests in this release; an application-detail live browser check requires a deliberately saved programme and is retained as outstanding QA rather than manufacturing student data.

The in-product language selector was then opened and switched to its existing Shami Arabic option without editing a student record or triggering an AI request. The RTL layout, Arabic navigation, header hierarchy, and First Research Set fallback displayed correctly. As with the English check, this account has no saved programme; the deterministic experience appropriately surfaced the Arabic first-research-set state instead of fabricating a Timeline or Application Detail.

## Change-of-direction recovery — non-mutating production check

Production deployment `dpl_Cu33jUZgmBv5aWdqxRMr613YSR5P` was checked at the public recovery route without writing, selecting, or submitting any student information. The first English navigation initially received a preceding cached Consultation shell from the canonical-origin PWA. After the application client updated while checking the Arabic route, a new cache-busting English navigation rendered the intended recovery surface.

The English screen displayed `Tell me what changed.`, a free-text textarea, an optional subject field whose helper states it does not change the saved direction, examples for Biotech, Anthropology, and rejection, a `Back to my journey` escape route, and `Continue the conversation`. It showed no programme cards, catalogue, recommendations, or option menu.

The Arabic `?recovery=1&lang=ar` route rendered a right-to-left Shami Arabic recovery surface with the equivalent private, conversation-first wording (`قلّي شو تغيّر.`), free-text and optional-subject prompts, Biotech/Anthropology/rejection examples, and a return action. Neither bilingual check triggered a network mutation, AI request, message, or profile/programme change.

## Requirement-first Documents release — non-mutating production check

The canonical authenticated `/dashboard?tab=documents` route was opened without uploading, linking, removing, or viewing a private file. The QA student has no private document and no saved programme, so the deterministic journey guard correctly redirected it to the preceding First Research Set state rather than fabricating a document requirement or application path. The new guard regression confirms that a student who already has at least one private document can enter the Documents surface before saving a programme; the surface is explicit that a file remains unlinked until the student deliberately relates it to an official saved-programme requirement.

The client bundle served by the canonical production origin was separately checked and contains the new `Keep the papers in their place.` Documents workspace copy. Its component and input-contract tests verify that links are named as student review context, never displayed as proof that a requirement is met, and never expose a submit or verification action. No private file, link, programme, profile, message, AI request, email, deadline, or application state was changed during QA.

## Core AI workflow and PWA activation release — non-mutating production check

The canonical Journey was reopened after release `dpl_5owMqQfoECpvm39N4rBwu9p94HB5` added explicit immediate PWA registration alongside the existing update policy. It rendered the current `WORK WITH NIGHTFALL` section with direct `Research with the Agent`, `Draft an essay`, and `Prepare outreach` entry points. This confirms the previously observed stale canonical shell was refreshed without requiring a student to clear browser data manually.

The English and Shami Arabic RTL canonical Research Agent routes were opened without entering or sending a question. Both rendered a free-text, source-backed Research Agent rather than a subject picker: the English route explicitly states `There is no subject menu to choose from`, while the Arabic route states `ما في قائمة تخصصات لازم تختار منها`. Both describe source-backed Germany programme evidence and show Nanotechnology as an example of an acceptable research direction. No profile, saved programme, source record, document, message, email, application state, or AI request was created or changed.

The QA account had neither a student Gemini key nor a configured platform provider. The deployed Research Agent correctly showed a truthful unavailable notice and disabled sending, rather than generating mock output. This is verified unavailable-state behavior only; a real Gemini answer was not requested or evaluated because doing so would require a user-owned or operator-provided live key and may incur usage.

## Gemini authorization-key compatibility — code and deployment check

The server-side encrypted BYOK schema was updated and deployed in `dpl_1vizV7CBbL8FyLX3LwrJiEfFr35a`. The isolated compatibility suite verifies acceptance of both the documented legacy `AIza` and newer `AQ.` Gemini key prefixes, while rejecting malformed `AQ` strings and unrelated provider-token formats. TypeScript passed; the relevant compatibility, fail-closed provider, and post-success accounting tests passed (5 tests across 3 files).

The canonical health endpoint responded successfully after deployment. No Gemini key was added to the sandbox, source repository, or document set; a live provider response was not requested, and no student record, message, email, application, or profile state changed during this validation.

## Google sign-in trigger repair — user-confirmed production check

Vercel production logs identified the Google sign-in callback failure as a database update error for an existing user record: the `trg_users_updated_at` trigger attempted to assign `NEW.updated_at`, while the persisted users table uses the quoted `"updatedAt"` column. The OAuth redirect and callback configuration were otherwise healthy.

The additive repair replaced only that users-table trigger with `nightfall_set_users_updated_at()`, which assigns `NEW."updatedAt" = NOW()`. The production trigger definition was checked read-only after migration. TypeScript and the focused Google configuration/trigger suite passed (4 tests). The user then retried the normal Google account sign-in and reported that it works. No new account, Gmail connection, email, application, or manual profile mutation was created during diagnosis.

## Direct Journey Settings control — production verification

Deployment `dpl_9b4zvJbNgUmQNpzYpceEJS43XB5o` reached `READY` and owns the canonical production alias. The Journey hero now includes a visible keyboard-accessible `Settings` control, paired with a clear purpose label for Gemini, Gmail, language, and privacy. It routes directly to protected `/settings` rather than remaining inside the optional Journey-areas menu. The corresponding Journey component regression passed, and a read-only canonical bundle check confirmed that both Settings strings are present in the live release.
