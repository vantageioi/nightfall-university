# Phase 0 Baseline Report — 2026-08-24

> **Purpose:** reproducible ground truth of the extracted tree *before* any
> architectural work. "Baseline" means truth, not achievement. Failures are
> recorded verbatim and classified; nothing was fixed during capture.

## Environment

| Item | Value |
|---|---|
| OS / shell | Windows (win32), PowerShell 5.1 |
| Node | v20.20.0 |
| pnpm | 10.4.1 (matches `packageManager` pin in package.json incl. sha512) |
| Working tree | pristine commit `cc6ad26` ("Baseline snapshot: pristine extracted tree") |

### Key resolved dependency versions (`pnpm ls --depth 0`)

- @trpc/* 11.18.0 · drizzle-orm 0.44.7 · drizzle-kit 0.31.10 · mysql2 3.23.3
- vite 7.1.9 · esbuild 0.25.10 · vitest 2.1.9 · typescript 5.9.3
- wouter **3.7.1 via patchedDependencies** (`patches/wouter@3.7.1.patch`) — do not bump casually

## Database / migration state (read-only inspection only)

- Migration files present: **19 SQL files, `drizzle/0000_friendly_mattie_franklin.sql` … `drizzle/0018_low_zombie.sql` (contiguous).**
- **`drizzle/meta/` does not exist** — no `_journal.json`. Consequence: `pnpm db:push`
  (`drizzle-kit generate && drizzle-kit migrate`) cannot correctly determine applied state
  until the journal is regenerated. **No repair was attempted during baseline** (constraint:
  read-only inspection only).
- Live DB inspection: connection attempt against configured `DATABASE_URL`
  (`localhost:3306/nightfall`) failed with **ECONNREFUSED**. No MySQL server reachable from
  this machine. Applied-migration state is therefore **UNVERIFIABLE here**; must be re-inspected
  on a machine where the database runs. Credentials were never printed.

## Baseline command results

> **⚠ Capture integrity note.** Two captures exist. During Capture 1 an external
> actor concurrently edited this directory (BYO-AI key resolution in
> `server/_core/llm.ts`, a new `server/domain/planLimits.ts`, schema/router
> changes) between the pristine commit and the gate runs. Capture 2 re-ran all
> gates against a detached worktree of exactly `cc6ad26`. **Capture 2 is the
> authoritative baseline.** Capture 1 numbers are retained below, clearly
> labeled, because honesty about measurement beats tidiness.
>
> **Capture 3 addendum (post-Phase-1a).** Gates re-run at commit `b2abfe6` in an
> isolated worktree with `pnpm install --frozen-lockfile`: typecheck **PASSES
> with zero errors**, tests 102/103 (sole environmental Gmail failure), build
> **GREEN** (2m37s). The 14 previously-reported client type errors do not exist
> under locked dependency versions — they were artifacts of version drift in the
> main tree's unlocked `node_modules` (tRPC inference types shifting with
> floating resolutions). **Rule going forward: authoritative gate runs use
> `--frozen-lockfile` installs in an isolated tree; the shared working tree is
> treated as unverified whenever another writer is active.**
>
> **Capture 4 — Postgres port (Phase 1.5).** Data layer ported MySQL→Postgres;
> production database is Supabase (session-pooler host unreachable from the dev
> network — transaction pooler :6543 in use, drivers pinned `prepare:false`).
> Gates at commit `cddb379`: typecheck exit 2 with **15 client-only errors**
> (zero server; legacy classics plus two files from the parallel stream's
> commits), tests **102/103**, build **GREEN**. Live smoke against Supabase:
> select/insert/delete through the app's own driver green; 34 tables +
> 17 `updated_at` triggers applied and journaled.

### Authoritative results — Capture 2 (worktree @ `cc6ad26`, clean)

| Gate | Result |
|---|---|
| `pnpm check` | **RED, exit 2 — 14 errors**, all client-side: `JourneyToolsLegacy.tsx` ×8 (Date-vs-string over superjson), `toggleLanguage` ×4 (Access/ConsultantOnboarding/StudentSettings/Waitlist), `main.tsx` transformer typing ×1, `DashboardLayout.tsx` handler misuse ×1. **Zero errors reference `planLimits.ts`** — proving those belonged entirely to the concurrent WIP file. |
| `pnpm test` | **RED, exit 1 — 99/100 passed.** Sole failure: `gmailConnection.test.ts`, environmental (missing `GMAIL_*` credentials). Identical to Capture 1. |
| `pnpm build` | **GREEN, exit 0** — client AND server bundle succeed (`built in 5m 30s`). The Capture 1 server-bundle failure was 100% contamination by the WIP `planLimits.ts`. |

Measurement notes for reproducibility: the worktree reused the main tree's
`node_modules` via an NTFS junction; under that setup plain `tsc --noEmit`
OOM'd at Node's default ~2 GB heap and required
`NODE_OPTIONS=--max-old-space-size=6144`. This is a junction-resolution
artifact, not a property of the repository (the same command ran normally in
the main tree). Raw Capture 2 outputs: `%TEMP%\opencode\nf-baseline\baseline2-{check,test,build}.txt`.

### Capture 1 (contaminated — retained for the record)

Raw outputs preserved during capture:
`%TEMP%\opencode\baseline-check.txt`, `baseline-test.txt`, `baseline-build.txt`,
`pnpm-ls.txt`, `inspect-migrations.mjs` (read-only inspector script).

### 1. `pnpm check` (tsc --noEmit) — **FAIL, exit code 2** *(Capture 1: 16 errors; 2 were concurrent-WIP)*

16 TypeScript errors observed at capture time across 8 files:

| File | Errors | Classification |
|---|---|---|
| `client/src/pages/JourneyToolsLegacy.tsx` | 8 (lines 124, 186×4, 187, 188, 191) | Real defect (type-level): Date-over-the-wire serialized as string vs `Date` field expectations |
| `server/domain/planLimits.ts` | 2 | **Real defect:** imports `aiUsageCounters` and `users` which `server/db.ts` does not export |
| `client/src/pages/Access.tsx:22` | 1 | Real defect: `toggleLanguage` missing from language context |
| `client/src/pages/ConsultantOnboarding.tsx:115` | 1 | Same as above |
| `client/src/pages/StudentSettings.tsx:10` | 1 | Same as above |
| `client/src/pages/Waitlist.tsx:36` | 1 | Same as above |
| `client/src/main.tsx:17` | 1 | Real defect: SuperJSON transformer type mismatch against tRPC 11.18 client typing |
| `client/src/components/DashboardLayout.tsx:223` | 1 | Real defect: mutation object passed where `MouseEventHandler` expected |

None were fixed during baseline. The `planLimits.ts` import breakage also blocks the
server production bundle (see below), so it is load-bearing, not cosmetic.

### 2. `pnpm test` (vitest run) — **FAIL, exit code 1 — 99/100 tests pass**

- Test files: **35 passed, 1 failed** (36 total). Tests: **99 passed, 1 failed** (100 total). Duration 58.6s.
- Sole failure: `server/gmailConnection.test.ts > Gmail OAuth configuration` —
  `Error: Gmail connection is not configured yet.` thrown by `getGmailOAuthConfig()`
  because `GMAIL_CLIENT_ID` / `GMAIL_CLIENT_SECRET` / 32+-char `GMAIL_TOKEN_ENCRYPTION_KEY`
  are absent in this machine's environment. **Environmental**, not a code regression;
  expected to pass wherever valid Gmail credentials are provisioned.
- Note: `server/auth.logout.test.ts` logged a DB `ECONNREFUSED` inside its logout flow but
  **passed** — it exercises the graceful-degradation path ("Session revocation failed during
  logout"). Consistent with the unreachable database above.

### 3. `pnpm build` (vite build && esbuild server bundle) — **FAIL, exit code 1** *(Capture 1 only — see integrity note)*

- Client: **GREEN** — `vite build` completed in 4m 1s (chunk-size warnings only, non-fatal).
- Server: **RED** — esbuild failed bundling `server/_core/index.ts`:
  ```
  X [ERROR] No matching export in "server/db.ts" for import "aiUsageCounters"
      server/domain/planLimits.ts:4:9
  X [ERROR] No matching export in "server/db.ts" for import "users"
      server/domain/planLimits.ts:4:33
  2 errors
  ```
  Same root cause as the two `planLimits.ts` typecheck errors.

## Overall baseline verdict *(authoritative: Capture 2)*

| Gate | Result |
|---|---|
| Typecheck | RED (14 errors, all client-side, all pre-existing) |
| Unit tests | RED (99/100; single failure environmental) |
| Production build | **GREEN** (client + server) |

Per Phase 0 constraint #6 this baseline **passes Phase 0**: every failure is accurately
captured and classified. First architectural work (Phase 1a+) proceeds against this truth.

**Carry-forward debt list (from authoritative Capture 2):**
`toggleLanguage` ×4 · Date-vs-string types in `JourneyToolsLegacy.tsx` ×8 ·
tRPC transformer typing in `main.tsx` ×1 · handler misuse in `DashboardLayout.tsx` ×1.
None block the build; all are scheduled as a standalone hygiene commit inside Phase 1,
separate from the boundary refactor. The concurrently-developed BYO-AI / planLimits
workstream lives uncommitted in the main tree (untouched by Phase 0) and must be parked
or committed by its author before Phase 1a begins, so boundary-refactor diffs stay attributable.

## Reproduction

```powershell
# On Windows, PowerShell 5.1+, Node v20.x, pnpm 10.4.1:
git worktree add --detach "$env:TEMP\opencode\nf-baseline" cc6ad26
# junction or install node_modules; then:
$env:NODE_OPTIONS="--max-old-space-size=6144"   # only needed with junctioned node_modules
pnpm check             # expect: FAIL exit 2, 14 errors as listed above
pnpm test              # expect: FAIL exit 1, 99/100 (gmail env-dependent)
pnpm build             # expect: PASS exit 0 (~5m30s)
```

Environment assumptions: no MySQL server required for `check`; `test` needs no DB
(one test degrades gracefully) except the Gmail credentials note above; `build` needs no DB.
